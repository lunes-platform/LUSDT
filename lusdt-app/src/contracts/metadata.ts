// Metadados dos contratos LUSDT — importados dos artefatos compilados
// Gerado automaticamente a partir dos arquivos .contract (cargo contract build)
import lusdtContractJson from './lusdt_token.contract.json';
import taxManagerContractJson from './tax_manager.contract.json';
import stakingManagerContractJson from './staking_manager.contract.json';

// Re-export o metadata completo do LUSDT Token
export const LUSDT_TOKEN_METADATA = lusdtContractJson;

// Re-export o metadata completo do Tax Manager
export const TAX_MANAGER_METADATA = taxManagerContractJson;

// Re-export o metadata completo do Staking Manager
export const STAKING_MANAGER_METADATA = stakingManagerContractJson;
